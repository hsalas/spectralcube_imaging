from setuptools import setup

setup(
    name='spectralcube_imaging',
    version='0.1',
    include_package_data=True,
    install_requires=[
        'numpy',
        'astropy',
        'scipy',
        'matplotlib'
    ]
)
